<!DOCTYPE html>
<html>
<head>
	<meta charset="utf-8">
	<meta http-equiv="X-UA-Compatible" content="IE=edge,chrome=1">
	<meta name="viewport" content="width=device-width, initial-scale=1.0">
  <!--[if lt IE 9]>
      <script src="http://html5shim.googlecode.com/svn/trunk/html5.js" type="text/javascript"></script>
  <![endif]-->

	<title><%= content_for?(:title) ? yield(:title) : "Valency online" %></title>

	<meta name="author" content="EVA MPG">
	<%= stylesheet_link_tag    "application", media:"all" %>
	<%= javascript_include_tag "application" %>
	<%= csrf_meta_tags %>
	<%= yield :head %>
</head>
<body data-controller="<%= controller_name %>" data-action="<%= action_name %>">

  <header class="navbar navbar-fixed-top">
    <div class="navbar-inner">
      <div class="container">
        <%= render 'layouts/navigation' %>
      </div>
    </div>
  </header>

  <div id="main" role="main">
    <div class="container">
      
      <%= render 'layouts/submenu' unless controller_name == 'meanings' or controller_name == 'languages' && action_name == 'index' %>

      <div class="row">
        <div class="span12">
          <%= render 'layouts/messages' %>
        </div>
      </div>

      <%= yield %>

    </div><!-- end of .container -->
  </div><!-- end of #main -->

  <footer>
    <!-- scripts for google maps go here -->
    <%= yield :scripts %>
  </footer>

</body>
</html>